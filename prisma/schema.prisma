generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                 String        @id @default(uuid())
  name               String
  mobile             String        @unique
  email              String?
  password           String?
  gender             String
  motherTongue       String
  country            String
  isPaid             Boolean       @default(false)
  role               String        @default("USER")
  status             String        @default("ACTIVE")
  createdAt          DateTime      @default(now())
  updatedAt          DateTime      @updatedAt
  isProfileCompleted Boolean       @default(false)
  unlockedBy         ContactView[] @relation("TargetRelation")
  unlockedProfiles   ContactView[] @relation("ViewerRelation")
  profile            Profile?
}

model ContactView {
  id        String   @id @default(uuid())
  viewerId  String
  targetId  String
  createdAt DateTime @default(now())
  target    User     @relation("TargetRelation", fields: [targetId], references: [id])
  viewer    User     @relation("ViewerRelation", fields: [viewerId], references: [id])

  @@unique([viewerId, targetId])
}

model Profile {
  id               String    @id @default(uuid())
  userId           String    @unique
  dob              DateTime?
  bio              String?
  photoUrl         String?
  religion         String?
  caste            String?
  birthStar        String?
  maritalStatus    String    @default("UNMARRIED")
  currentResidence String?
  location         String?
  occupation       String?
  consent          Boolean   @default(false)
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
  qualification    String?
  denomination     String?
  dosham           String?
  user             User      @relation(fields: [userId], references: [id], onDelete: Cascade)
}
